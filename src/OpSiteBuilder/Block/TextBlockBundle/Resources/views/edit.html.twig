{% extends app.request.isXmlHttpRequest ? 'OpSiteBuilderWebBundle:Block:layout_ajax.html.twig' : 'OpSiteBuilderWebBundle:Block:layout.html.twig' %}

{% block content %}
    {%  include 'OpSiteBuilderWebBundle:Block/Tools:_basic_edit_tools.html.twig' with {
        block: block
    } %}

    <div ng-cancel-drag class="block-content">
        <form ng-submit="simpleSubmit($event)">
            {{ form_row(form.title) }}
            {{ form_row(form.content) }}
            {{ form_rest(form) }}

            <input type="submit" value="Save" />
        </form>
    </div>
{% endblock %}
